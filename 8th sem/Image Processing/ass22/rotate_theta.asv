function [ new_img ] = rotate_theta( image, theta )
[row,col,channels]=size(img);
new_img = double(zeros(row,col,1));
for i=1:row
    for j=1:col
        new_i = int32((
        new_img(new_i,new_j) = img(i,j);
    end
end
new_img = uint8(new_img);
end

